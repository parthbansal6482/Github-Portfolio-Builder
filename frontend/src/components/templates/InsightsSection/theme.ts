// Shared theme props that every insight component accepts
export interface InsightTheme {
    accentColor: string;
    textColor: string;
    secondaryTextColor: string;
    bgColor: string;
    cardBg: string;
    cardBorder: string;
    fontFamily: string;
}
